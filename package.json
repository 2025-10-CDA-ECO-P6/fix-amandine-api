{
  "name": "amandine-real-time-room",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel --filter api --filter web-app run dev",
    "dev:api": "pnpm --filter api dev",
    "dev:web": "pnpm --filter web-app dev",
    "build": "pnpm -r --filter api --filter web-app run build",
    "build:api": "pnpm --filter api build",
    "build:web": "pnpm --filter web-app build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.28.0"
}
